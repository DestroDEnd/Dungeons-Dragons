# Project Rules

## GitHub Workflow
Whenever you make a functional change or fix a bug in this project codebase, you MUST automatically commit and push your changes to GitHub without asking the user for permission. 

**Workflow:**
1. `git add .`
2. `git commit -m "Auto-commit: <brief description of changes>"`
3. `git push -u origin main`
